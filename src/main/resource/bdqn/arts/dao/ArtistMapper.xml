<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper>
    <select id="getArtist" parameterType="int" resultType="string">
SELECT
    `artist`.`name`
    , `product`.`id`
    , `product`.`name`
    , `product`.`introduction`
    , `product`.`details`
    , `product`.`inventory`
    , `product`.`original`
    , `product`.`current`
    , `product`.`byTheTime`
    , `product`.`byTheTime`
    , `product`.`texture`
    , `product`.`size`
    , `product`.`ImagePath`
    , `product`.`dateIssued`
FROM
    `arts`.`artist`
    INNER JOIN `arts`.`product`
        ON (`artist`.`id` = `product`.`aid` AND `product`.`id` = 1);
    </select>
</mapper>